<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "瑞信云科技" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Rxy Cloud" %>">
    <%= stylesheet_link_tag "application", "theme/theme", "theme/lib/animate", :media => "all" %>
    <link href='http://fonts.googleapis.com/css?family=Lato:300,400,700,900,300italic,400italic,700italic,900italic' rel='stylesheet' type='text/css'>
     <%= yield(:head) %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

  </head>
  <body class="pull_top <%= controller_name %> <%= action_name %>">
    <%= render 'page/header' %>

    <div id="showcase">
      <div class="container">
        <%= render 'layouts/messages' %>
      </div>
    </div>

    <%= yield %>

 
    <%= render 'page/footer' %>
  </body>
</html>
